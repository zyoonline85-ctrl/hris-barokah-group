name: hris_employee
description: Aplikasi HRIS Karyawan untuk absensi, cuti, dan penggajian.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  
  # API & State
  http: ^1.2.0
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  
  # Kehadiran & Formatter
  geolocator: ^11.0.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
